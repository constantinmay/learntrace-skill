diff --git a/src/roster_import.py b/src/roster_import.py
index 2e771c1..9ad3f08 100644
--- a/src/roster_import.py
+++ b/src/roster_import.py
@@ -1,4 +1,14 @@
-ROW = re.compile(r"([^,]+),([^,]+),([^,]+)")
+def split_csv_row(row: str) -> list[str]:
+    fields, current, quoted = [], [], False
+    for char in row:
+        if char == '"':
+            quoted = not quoted
+        elif char == ',' and not quoted:
+            fields.append(''.join(current))
+            current = []
+        else:
+            current.append(char)
+    fields.append(''.join(current))
+    return fields
diff --git a/tests/test_roster_import.py b/tests/test_roster_import.py
index 7036cc2..9bc3f26 100644
--- a/tests/test_roster_import.py
+++ b/tests/test_roster_import.py
@@ -3,3 +3,9 @@
 def test_import_simple_player():
     assert split_csv_row("Alice,Club A,100") == ["Alice", "Club A", "100"]
+
+def test_import_player_with_quoted_comma():
+    assert split_csv_row('"Smith, Jr.",Club B,200') == ["Smith, Jr.", "Club B", "200"]
+
+def test_import_empty_salary():
+    assert split_csv_row("Bob,Club C,") == ["Bob", "Club C", ""]
