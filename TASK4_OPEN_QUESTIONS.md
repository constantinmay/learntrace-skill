# Task 4 Open Questions

更新时间：2026-08-20

## 当前遗留问题

- 项目目标目前从 README、任务或要求类文档事件中保守提取；没有明确
  证据时显示“未记录”。后续可考虑增加独立的结构化目标输入。
- 个人反思和后续学习保留为学生本人填写区域；当前一次性 archive CLI
  不提供交互式编辑器。
- AI 轨迹范围过滤依赖安全路径占位符、低信号命令表、候选依据关系和
  Skill 自身路径标记；新增适配器或工具类型时需要同步扩充规则。
- AI 使用章节最多展示 20 条相关轨迹，其余数量会在档案中明确标注。
- 候选问题最多保留 10 条，并会先按类型、陈述和问题去重；真实项目是否
  需要更低的默认上限，仍应通过更多样例评估。
- OpenAI-compatible LLM 的首轮候选仍可能因模型而变化；空结果、无效结果或
  调用失败会留下告警并回退到本地确定性规则。确认阶段固定使用首次归档
  快照，不会再次推理。
- OpenCode 宿主模型和 LearnTrace 候选推断器是两层独立模型。真实 Skill
  联调已验证授权门控、标准归档和问题交接，但宿主模型仍可能在已授权目录中
  进行多余的文件名查询；Skill 不能替代 OpenCode 工具权限和用户审批。
- 仓库尚未配置 GitHub Actions 自动 CI；合并前仍需人工执行锁文件、Ruff、
  Pyright、Pytest、pre-commit 和端到端检查。

## 已验证能力

- 默认确定性推断可重复生成候选；显式启用的 OpenAI-compatible LLM 候选
  只有通过 schema 和依据校验后才会进入档案。
- 只有工具名称、命令类型或路径的最小轨迹不会单独生成学习节点；缺少充分
  证据时系统宁可少问，也不把时间邻近写成学习关系。
- LLM 路径只在明确启用并配置 API key 时运行。
- 学生确认会应用到首次保存的候选快照，不会覆盖 Task 3 或重新调用 LLM。
- 机器可读档案保留事实、候选、确认、告警、来源索引与稳定内容指纹。
